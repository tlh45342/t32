# t32-nm versions

## 0.0.1

- Initial T32OBJ v1 reader.
- Displays sections, symbols, and relocations.
- Adds focused table-selection options.
- Validates file bounds, indexes, strings, alignments, reserved fields, and relocation patch ranges.
- Adds Windows/Linux build, test, and install targets.

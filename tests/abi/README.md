# T32 ABI 0.1 Validation

Eight independent executable tests for argument registers, return values, caller/callee preservation, stack restoration, alignment, nested calls, and stack arguments.

Run all tests with:

```text
make test
```
